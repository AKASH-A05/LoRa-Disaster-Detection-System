# LoRa-Disaster-Detection-System
Long-range disaster detection system using ESP32 (transmitter) and Arduino (receiver) with LoRa SX1278. Collects temperature, humidity, gas, flame, and vibration data, transmits packets over LoRa, and triggers alerts on the receiver side for real-time hazard monitoring.
